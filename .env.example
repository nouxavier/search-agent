# Copie para .env e preencha. O .env real é ignorado pelo git.
#   cp .env.example .env

# Credencial da API Anthropic (usada por `agent smoke` e, a partir da E2, por reflect/consolidate).
ANTHROPIC_API_KEY=sk-ant-...
